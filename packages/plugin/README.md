# Figma Plugin

Default plugin assets live in `figma-plugin/`.

`vendor-figma-bridge/` contains the Figma Bridge plugin implementation as a migration reference for high-fidelity selection extraction, image export, and SVG export.

The product plugin should eventually combine:

- Figma Bridge level selection extraction
- Figma HTML Loop selection confirmation UX
- Figma HTML Loop patch apply support
